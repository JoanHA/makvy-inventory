/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ['firebasestorage.googleapis.com']  // Replace 'your-image-domain.com' with your actual image domain
    }
};

export default nextConfig;
